PROGRAM EXCERCISE;

VAR A : ARRAY(. 10 .) OF INTEGER;
	MIN : INTEGER;
    I : INTEGER;
    J : INTEGER;
    CHECK : INTEGER;
     
BEGIN
    
    FOR I := 1 TO 10 DO
        A(.I.) := READI;
    
    MIN := A(.1.);
    FOR I := 2 TO 10 DO
        If A(.I.) < MIN Then MIN := A(.I.);
    CALL WRITEI(MIN);
    
    CHECK := 0;
    FOR I := 1  TO 10 DO
        FOR J:= I+1 TO 10 DO
        BEGIN
            If A(.I.) = A(.J.) Then CHECK := 1;
        END;
    CALL WRITELN;
    
    IF CHECK = 1 THEN	
    	CALL WRITEC('Y');
END.